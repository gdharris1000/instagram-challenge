<img src="<%=(url_for(@post.photo))%>">
    <p>
        <%=@post.caption%>
    </p>
    <p><%= link_to "update caption", edit_post_path %></p>
    <p><%= link_to "Delete post", post_path(@post), method: :delete, data: { confirm: "Are you sure?" } %></p>